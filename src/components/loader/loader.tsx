import { LoaderWrapper, StyledLoader } from "./style";

export const Loader = () => {
  return (
    <LoaderWrapper>
      <StyledLoader>
        <div></div>
        <div></div>
      </StyledLoader>
    </LoaderWrapper>
  );
};
